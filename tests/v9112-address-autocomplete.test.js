'use strict';
const assert=require('node:assert/strict'),fs=require('node:fs'),path=require('node:path'),suggest=require('../js/address-suggestions.js');
const root=path.join(__dirname,'..'),read=file=>fs.readFileSync(path.join(root,file),'utf8');
const settings={cities:['Таромське','Дніпро'],streets:{Таромське:['Центральна','Шевченка']}},tickets=[{city:'таромське',street:'Польова'},{city:'Кам’янське',street:'Нова'}],snapshot=JSON.stringify({settings,tickets});
assert.deepEqual(suggest.cities(settings,tickets,'Та'),['Таромське']);assert.deepEqual(suggest.cities(settings,tickets,'дНі'),['Дніпро']);
assert.deepEqual(suggest.streets(settings,tickets,'ТАРОМСЬКЕ',''),['Центральна','Шевченка','Польова']);assert.deepEqual(suggest.streets(settings,tickets,'Таромське','пол'),['Польова']);
assert.equal(suggest.streetAfterCitySelection(settings,tickets,'Таромське','Шевченка'),'Шевченка','valid existing street is preserved');
assert.equal(suggest.streetAfterCitySelection(settings,tickets,'Дніпро','Шевченка'),'','street from another city is cleared');
assert.deepEqual(suggest.streets(settings,tickets,'Unknown',''),[]);assert.deepEqual(suggest.streets(settings,tickets,'Кам’янське','x'),[]);assert.equal(JSON.stringify({settings,tickets}),snapshot,'source address data is not mutated');
const html=read('index.html'),bindings=read('js/tickets-bindings.js'),render=read('js/settings-render.js'),styles=read('styles.css'),sw=read('sw.js');
assert.match(html,/autocomplete="one-time-code"[^>]*role="combobox"/,'anti-browser-address-autofill semantics remain');assert.doesNotMatch(html,/\slist="(?:city|street)Datalist"/,'native datalist cannot auto-fill a suggestion');assert.match(html,/citySuggestions[\s\S]*streetSuggestions/);
assert.match(bindings,/data-address-suggestion/);assert.match(bindings,/if\(kind==='city'\)/);assert.match(bindings,/streetAfterCitySelection/);assert.doesNotMatch(bindings,/input\.value=values\[0\]|street\.value=values\[0\]/,'first suggestion is never selected automatically');
assert.match(bindings,/cityAddressInput\.addEventListener\('focus',\(\)=>renderAddressSuggestionMenu\('city'\)\)/,'city suggestions open on focus');assert.match(bindings,/cityAddressInput\.addEventListener\('input',\(\)=>renderAddressSuggestionMenu\('city'\)\)/,'city typing filters suggestions');
const citySelect=bindings.slice(bindings.indexOf("if(kind==='city')"),bindings.indexOf("}else{input.value=value"));assert.match(citySelect,/closeAddressSuggestionMenus\(\)/,'city suggestions close after explicit selection');assert.doesNotMatch(citySelect,/street\.focus\(|renderAddressSuggestionMenu\('street'\)/,'city selection neither focuses street nor opens street suggestions');
assert.match(bindings,/\['pointerdown','pointerup','touchstart','touchend'\][\s\S]*?guardAddressSuggestionTap/,'the complete mobile pointer/touch sequence is contained');assert.match(bindings,/addEventListener\('click'[\s\S]*?stopImmediatePropagation\(\)[\s\S]*?closeAddressSuggestionMenus\(\)[\s\S]*?f_house/,'selection completes on the owned click before the tray closes and street advances to house');
assert.match(bindings,/streetAddressInput\.addEventListener\('focus',\(\)=>renderAddressSuggestionMenu\('street'\)\)/,'street suggestions open on explicit focus');assert.match(bindings,/streetAddressInput\.addEventListener\('input',\(\)=>renderAddressSuggestionMenu\('street'\)\)/,'street typing filters suggestions');
assert.match(bindings,/addressAutocompleteBound/,'repeat screen binding does not duplicate listeners');assert.match(bindings,/visualViewport\?\.addEventListener\('resize'/);assert.match(bindings,/visualViewport\?\.addEventListener\('scroll'/);assert.match(bindings,/window\.addEventListener\('resize',repositionAddressSuggestionMenus\)/,'orientation/layout resize recalculates placement');
assert.match(render,/window\.innerHeight-visibleBottom/,'tray bottom follows the keyboard-visible viewport');assert.match(render,/height\*\.34/);assert.match(render,/trayTop/);assert.match(render,/getBoundingClientRect\(\)/,'input overlap is detected without anchoring tray to the field');assert.match(render,/scrollIntoView\(\{block:'center',inline:'nearest'\}\)/,'active form field is moved clear of the tray');
assert.match(styles,/\.address-suggestions\{position:fixed;z-index:2400/);assert.match(styles,/top:auto/);assert.match(styles,/bottom:var\(--address-suggestions-bottom/);assert.match(styles,/max-height:var\(--address-suggestions-max-height/);assert.match(styles,/overflow-y:auto/);assert.match(styles,/min-height:48px/);
assert.match(sw,/address-suggestions\.js/,'offline app shell includes custom autocomplete');
console.log('PASS explicit-tap-only offline autocomplete, city-safe street lifecycle, keyboard-aware panel and anti-autofill semantics');
