'use strict';
const assert=require('node:assert/strict'),fs=require('node:fs'),path=require('node:path'),root=path.join(__dirname,'..'),read=p=>fs.readFileSync(path.join(root,p),'utf8');
const files=['app.js',...fs.readdirSync(path.join(root,'js')).filter(f=>f.endsWith('.js')).map(f=>`js/${f}`)],source=files.map(read).join('\n'),html=read('index.html');
for(const name of ['formatUaDate','parseBackupNote','showVizitka','showDogovor','printDogovorAsPdf','buildDogovorText','shareTicket','copyTicketText','sharePhoto','shareCurrentTicket','openExportModal','downloadExport','openImportModal','dedupTickets','repairCorruptedTickets','runBulkImport','openReportModal','renderReport'])assert.equal((source.match(new RegExp(`(?:async\\s+)?function\\s+${name}\\s*\\(`,'g'))||[]).length,1,`${name} must have one owner`);
for(const name of ['loadSettings','saveSettings','ensureCatalogTags','syncCatalogTagState','backfillAddressDictionariesFromTickets','showAppsScriptModal','bindSettingsScreen'])assert.equal((source.match(new RegExp(`(?:async\\s+)?function\\s+${name}\\s*\\(`,'g'))||[]).length,1,`${name} must have one owner`);
for(const name of ['fetchPhotoFromTelegram','collectLocalPhotoData','getPhotoCached','resolvePhotoAsync','storePhoto','deletePhotoKey','clearAllPhotos','migrateLegacyPhotosToIdb','sendToTelegramChat','backupTicketToTelegram','backupTicketToTelegramNow','restoreTicketFromTelegramJson','sendTelegramTestMessage','runBulkTelegramJob'])assert.equal((source.match(new RegExp(`(?:async\\s+)?function\\s+${name}\\s*\\(`,'g'))||[]).length,1,`${name} must have one owner`);
for(const name of ['renderShiftCalendar','renderShiftsScreen','addShift','deleteShift','buildShiftMonthReport','buildCurrentMonthShiftsTelegramText','syncShiftsMonthlyTelegramMessage','shareMonthShifts','sendShiftsReportToTelegram','bindShiftsScreen'])assert.equal((source.match(new RegExp(`(?:async\\s+)?function\\s+${name}\\s*\\(`,'g'))||[]).length,1,`${name} must have one owner`);
for(const name of ['blankCalcState','resetCalcForm','loadTicketIntoForm','saveTicketFromForm','renderTicketsScreen','renderMainTicketList','deleteTicket','restoreDeletedTicket','editTicket','renderCalendar','bindTicketsScreen','bindCalculatorScreen','startMacScan','stopMacScan'])assert.equal((source.match(new RegExp(`(?:async\\s+)?function\\s+${name}\\s*\\(`,'g'))||[]).length,1,`${name} must have one owner`);
for(const name of ['findNaryadMatches','showNaryadQueue','showAddNaryadModal','showNaryadChecker','startNewTicketFlow','openAddressNavigator','showEditAbonentProfile','renderAddressNav','attachAddressNavHandlers'])assert.equal((source.match(new RegExp(`(?:async\\s+)?function\\s+${name}\\s*\\(`,'g'))||[]).length,1,`${name} must have one owner`);
assert.ok(html.indexOf('js/settings-core.js')<html.indexOf('app.js'));assert.ok(html.indexOf('js/app-format-utils.js')<html.indexOf('app.js'));assert.ok(html.indexOf('app.js')<html.indexOf('js/qr-share-domain.js'));assert.ok(html.indexOf('js/photo-telegram-domain.js')<html.indexOf('js/security-telegram.js'));
assert.ok(html.indexOf('js/ticket-editor-core.js')<html.indexOf('app.js'));assert.ok(html.indexOf('app.js')<html.indexOf('js/ticket-editor-domain.js'));assert.ok(html.indexOf('js/ticket-editor-domain.js')<html.indexOf('js/tickets-bindings.js'));
assert.ok(html.indexOf('js/tickets-bindings.js')<html.indexOf('js/ticket-address-domain.js'));assert.ok(html.indexOf('js/ticket-address-domain.js')<html.indexOf('js/security-hardening.js'));
assert.equal(/legacy(?:OpenSetPasswordModal|EnsureAppUnlocked|ShowLockScreen)Disabled/.test(source),false,'retired base lock owners removed');
console.log('PASS decomposition owners and bootstrap order');
